import styled from '@emotion/styled'

export const Form = styled.form`
  position: absolute;
  bottom: 30px;
  left: 50%;
  transform: translateX(-50%);
  width: 100%;

  text-align: center;

  button {
    margin-top: 24px;
  }
`
